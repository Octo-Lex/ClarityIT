# ClarityIT v2 rendered distributions — v0.1

These DOCX files are rendered distributions of the Markdown specifications in
[`docs/v2/`](../../).

The Product Definition embeds the corrected layered architecture image. Its
footer states:

> **Execution truth invariant:** Provider, worker and agent outputs remain
> source-attributed claims after persistence. Only independent verification can
> establish a verified result, and only a separate outcome decision can accept
> it.

Do not edit the DOCX files independently. Revise the canonical Markdown,
approve and pin its digest, then regenerate the distributions.
