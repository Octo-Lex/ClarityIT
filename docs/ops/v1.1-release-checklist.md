# ClarityIT v1.1.0 Release Checklist

## Pre-Release

- [ ] All 7 tracks accepted (Tracks 1–7)
- [ ] Track 8 (this checklist) completed
- [ ] Release notes written (`docs/releases/v1.1.0.md`)
- [ ] Risk acceptance v1 marked CLOSED (`docs/security/risk-acceptance-v1.md`)
- [ ] Git branch clean, all changes committed

## Migrations

- [ ] Migration 026 (`proxmox_mutation_windows`) applied
- [ ] Migration 027 (`expiring_notified_at`) applied
- [ ] Migration 028 (`user_webauthn_credentials`) applied

## Testing

- [ ] `go test -p 1 -count=1 -timeout 300s ./...` — all pass (378+)
- [ ] `npx vitest run` — all pass (64+)
- [ ] `pytest` (Python model gateway) — all pass (9+)
- [ ] `npx playwright test --workers=1` — all pass (19+)
- [ ] `go vet ./...` — clean

## Dependency Audit

- [ ] `npm audit --audit-level=high` → 0 vulnerabilities
- [ ] `npm audit --omit=dev --audit-level=high` → 0 vulnerabilities
- [ ] `pip check` — no broken requirements

## Production Image Verification

- [ ] Web container runs `nginxinc/nginx-unprivileged:alpine` (non-root, uid=101)
- [ ] Web container has `read_only: true` and `no-new-privileges:true`
- [ ] Production web image contains NO node/npm runtime
- [ ] API container runs as non-root

## Core Deployment Health

- [ ] `docker compose ps` — all core services healthy (8 containers)
- [ ] `curl -sf http://<your-host>:8765/health` → `{"status":"ok"}`
- [ ] `curl -sf http://<your-host>:3000` → HTML response
- [ ] PostgreSQL healthy
- [ ] NATS JetStream healthy
- [ ] Redis healthy
- [ ] MinIO healthy

## Agent Profile Health

- [ ] `docker compose --profile agent up -d` — reasoning worker starts
- [ ] `docker compose --profile agent ps` — reasoning worker healthy
- [ ] `WORKER_TOKEN` valid and accepted
- [ ] Reasoning worker validates forbidden env vars at startup

## v1.1 Feature Verification

### Backup Status (Track 3)
- [ ] `GET /api/admin/backup-status` returns PostgreSQL and MinIO file info
- [ ] Age status color correct (green ≤24h, yellow ≤72h, red >72h)
- [ ] Backup Status card visible in AdminOps
- [ ] No raw filesystem paths exposed (basename only)

### Mutation Windows (Track 2)
- [ ] `POST /api/admin/proxmox/mutation-window` creates a window
- [ ] `GET /api/admin/proxmox/mutation-window` returns active window
- [ ] `POST /api/admin/proxmox/mutation-window/{id}/close` closes window
- [ ] One active window enforced
- [ ] Duration bounded 1–60 min
- [ ] `PROXMOX_MUTATION_ENABLED=false` by default

### Approval Monitor (Track 4)
- [ ] Approval monitor running (structured slog output)
- [ ] Expiring approval fires one-time notification (`expiring_notified_at` set)
- [ ] Expired approval transitions to `expired` status
- [ ] No duplicate expiring notifications
- [ ] Frontend shows expiry badges and time remaining

### WebAuthn (Track 5)
- [ ] `WEBAUTHN_ENABLED=false` by default
- [ ] WebAuthn endpoints return 503 when disabled
- [ ] Production config validation rejects `http://` origin
- [ ] Development config allows `localhost` http
- [ ] Security page shows WebAuthn section
- [ ] No credential secrets rendered in UI

### Dry-Run Previews (Track 6)
- [ ] `dry_run=true` returns preview without creating action/approval
- [ ] Preview shows risk level, MFA requirement, approval requirement
- [ ] Preview shows mutation window and feature flag status
- [ ] Preview audit event (`asset.action.previewed`) has `dry_run:true`
- [ ] No side effects (no asset_actions row, no approval_request)

### Metrics (Track 7)
- [ ] `GET /api/admin/metrics` returns all 4 sections
- [ ] Metrics computed from PostgreSQL (no Redis cache)
- [ ] Permission-gated (403 for non-admin)
- [ ] No sensitive fields in response
- [ ] `/admin/metrics` page renders with summary cards

## Security Final Checks

- [ ] No secrets/PII in audit logs
- [ ] No secrets/PII in outbox events
- [ ] No secrets/PII in NATS messages
- [ ] No secrets/PII in Redis pub/sub
- [ ] No secrets/PII in UI
- [ ] No raw action_target in audit/outbox
- [ ] A5 autonomy hardcoded rejection (unchanged)
- [ ] Mutation disabled by default
- [ ] WebAuthn disabled by default unless configured
- [ | All high-risk actions require MFA + approval + policy + audit + outbox

## Post-Release

- [ ] Git tag created: `v1.1.0-operational-refinement`
- [ ] Release notes published
- [ ] Risk acceptance document closed
- [ ] All test evidence captured
