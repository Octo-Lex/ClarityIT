# ClarityIT v1.2.0 Release Checklist

## Pre-Release

- [x] All 7 tracks accepted (Tracks 1–7)
- [x] Track 8 (this checklist) completed
- [x] Release notes written (`docs/releases/v1.2.0.md`)
- [x] Risk acceptance v1 confirmed closed (`docs/security/risk-acceptance-v1.md`)
- [x] Security docs updated for v1.2 additions
- [x] Git branch clean, all changes committed

## Migrations

- [x] Migration 029 (`recommendation_evidence`) applied
- [x] Migration 030 (`action_outcomes`) applied
- [x] Migration 031 (`context_relation_reviews`) applied
- [x] Migration 032 (`agent_evaluation_runs`, `agent_evaluation_scenario_results`) applied

## Testing

- [x] `go test -p 1 -count=1 -timeout 300s ./...` — all pass (492)
- [x] `npx vitest run` — all pass (123)
- [x] `pytest` (Python model gateway) — all pass (9)
- [x] `npx playwright test --workers=1` — all pass (19)
- [x] `go vet ./...` — clean

## Dependency Audit

- [x] `npm audit --audit-level=high` → 0 vulnerabilities
- [x] `npm audit --omit=dev --audit-level=high` → 0 vulnerabilities
- [x] `pip check` — no broken requirements

## Production Image Verification

- [x] Web container runs `nginxinc/nginx-unprivileged:alpine` (non-root, uid=101)
- [x] Web container has `read_only: true` and `no-new-privileges:true`
- [x] Production web image contains NO node/npm runtime
- [x] API container runs as non-root

## Core Deployment Health

- [x] `docker compose ps` — all core services healthy (8 containers)
- [x] `curl -sf http://<your-host>:8765/health` → `{"status":"ok"}`
- [x] `curl -sf http://<your-host>:3000` → HTML response
- [x] PostgreSQL healthy
- [x] NATS JetStream healthy
- [x] Redis healthy
- [x] MinIO healthy

## Agent Profile Health

- [x] `docker compose --profile agent up -d` — reasoning worker starts
- [x] `docker compose --profile agent ps` — reasoning worker healthy
- [x] `WORKER_TOKEN` valid and accepted
- [x] Reasoning worker validates forbidden env vars at startup

## v1.2 Feature Verification

### Evidence Packs (Track 1)
- [x] `GET /api/teams/{teamId}/recommendations/{recommendationId}/evidence` returns evidence
- [x] Confidence score bounded 0.0–1.0
- [x] Stale evidence visible with `is_stale: true`
- [x] Evidence persisted by Go control plane (Python cannot persist)
- [x] Evidence sanitization strips sensitive patterns
- [x] EvidencePanel renders in RemediationPanel

### Incident Patterns (Track 2)
- [x] `GET /api/teams/{teamId}/incidents/patterns` returns patterns
- [x] 4 pattern types: recurring_asset, recurring_symptom, cluster, noisy_asset
- [x] Pattern detection is read-only (no mutations, no auto-remediation)
- [x] Computed from PostgreSQL at request time (no Redis cache)
- [x] Confidence bounded 0.0–1.0
- [x] No raw incident bodies in response
- [x] PatternCards renders with advisory badges

### Policy Simulation (Track 3)
- [x] `POST /api/admin/approval-policy/simulate` returns simulation results
- [x] `simulation_only=true` always
- [x] No live policy mutation
- [x] No approval request/decision creation
- [x] No Tool Gateway call
- [x] No audit/outbox emission
- [x] PolicySimulationPanel renders with NO save/apply/deploy buttons

### Risk Scoring (Track 4)
- [x] `GET /api/teams/{teamId}/assets/{assetId}/risk-score?action=...` returns score
- [x] Score bounded 0–100, advisory only
- [x] Does NOT bypass approval/MFA/policy/mutation-window
- [x] Dry-run preview includes risk_score object
- [x] RiskScoreDisplay component in AssetActions preview

### Outcome Tracking (Track 5)
- [x] `POST/GET .../asset-actions/{actionId}/outcome` works
- [x] `POST/GET .../remediations/{proposalId}/outcome` works
- [x] Terminal status required
- [x] Outcome recording creates NO retries or follow-up actions
- [x] No Tool Gateway call on outcome recording
- [x] Sensitive field sanitization (key=value patterns)
- [x] OutcomePanel renders with "no automatic retry" warning

### Context Quality (Track 6)
- [x] `GET /api/teams/{teamId}/context/quality` returns quality report
- [x] Confirm/dismiss endpoints record review state
- [x] Confirm/dismiss does NOT delete or rewrite graph data
- [x] Quality score bounded 0–100
- [x] Advisory-only behavior preserved
- [x] No Python worker call, no Tool Gateway call
- [x] ContextQualityPanel renders with advisory warning

### Agent Evaluation (Track 7)
- [x] `POST /api/admin/agent-evaluation/run` evaluates 5 golden scenarios
- [x] `GET /api/admin/agent-evaluation/results` returns latest run
- [x] `GET /api/admin/agent-evaluation/runs/{runId}` returns run detail
- [x] Evaluation does NOT call Tool Gateway
- [x] Evaluation does NOT create approval_requests, asset_actions, remediation_proposals
- [x] Evaluation does NOT mutate incidents/assets/context graph
- [x] Scores bounded 0.0–1.0 per dimension
- [x] Deterministic results for fixture scenarios
- [x] chain_of_thought never returned or persisted
- [x] AgentEvaluationPanel renders with evaluation-only warning

## Security Final Checks

- [x] No secrets/PII in audit logs
- [x] No secrets/PII in outbox events
- [x] No secrets/PII in NATS messages
- [x] No secrets/PII in Redis pub/sub
- [x] No secrets/PII in UI
- [x] No raw action_target in audit/outbox
- [x] No chain_of_thought in logs/audit/outbox/UI
- [x] A5 autonomy hardcoded rejection (unchanged)
- [x] No new mutation classes
- [x] No autonomy expansion
- [x] Mutation disabled by default
- [x] All high-risk actions require MFA + approval + policy + audit + outbox
- [x] Risk scoring does not bypass any control
- [x] Policy simulation does not mutate live data
- [x] Evaluation mode is non-executing

## Post-Release

- [x] Git tag created: `v1.2.0-operational-intelligence`
- [x] Release notes published
- [x] Risk acceptance document confirmed closed
- [x] All test evidence captured
- [x] Presenton confirmed absent from v1.2 runtime
