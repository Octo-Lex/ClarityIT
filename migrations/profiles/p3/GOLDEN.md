# P3 Golden Profile

The golden manifest (`golden-manifest.json`) and fingerprint below are the
expected result of capturing the P3 fixture. The migration runner and CI must
verify that a fresh P3 build produces this fingerprint.

- **fingerprint_sha256:** `3ef228814184a20b1d0162bc0aff4395b474a3dbadf4e51c1e102963b5537be4`
- **profiler_version:** `3.0.0-p1p2`
- **relations:** 65
- **source:** synthetic (generated by `scripts/profile/generate_p3.py` from P1 manifest)

## Validation (proven 2026-07-31)

1. **Self-consistent:** stored fingerprint == recomputed fingerprint.
2. **Deterministic:** two captures on fresh PG16 produce identical fingerprint.
3. **Secret-scan:** no passwords, tokens, credentials, production IPs, or production emails. Only schema column/table names match patterns (e.g. `password_hash` column name) — no values.
4. **Matches committed golden:** re-capture == `golden-manifest.json` fingerprint.

## What P3 is NOT

- Not a resolution of migrations 016/018/029 (G2 work).
- Not a competing 005/018-shaped variant — it represents the single captured P1 source shape.
- Not production evidence — it is a CI/conformance fixture.
